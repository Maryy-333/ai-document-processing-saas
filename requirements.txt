# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
# Phase 12: bumped from 0.0.9, which is affected by CVE-2024-53981 (High,
# CVSS 7.5 - DoS via inefficient handling of malformed multipart/form-data
# boundary data) - directly relevant since this app accepts file uploads.
# 0.0.28 also picks up fixes for PYSEC-2026-3038 (preamble/epilogue DoS) and
# CVE-2026-42561 (unbounded part headers). Not pinning the very latest
# release (0.0.32 at time of writing) since 0.0.28 already contains every
# specific fix found relevant here and later versions are perf-only changes.
python-multipart==0.0.28

# --- Configuration & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database (foundation only in Phase 2; models/migrations in Phase 3) ---
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
alembic==1.13.2

# --- PDF processing ---
PyMuPDF==1.24.9

# --- OCR (Phase 6) ---
# paddlepaddle 3.1.1: confirmed Linux wheels for cp311/cp312 (project's
# actual Python versions; the Phase 6 brief's "3.13" claim did not match
# the repository's actual Dockerfile/environment - verified before pinning).
# paddleocr 3.2.0: first release documented as fully supporting PaddlePaddle
# 3.1.x and with Python 3.12 restrictions relaxed (earlier 3.0.x releases
# had reported Python 3.12 friction). Not pinning the newest 3.6.x release,
# which adds a much larger doc-parsing surface we don't need for OCR.
paddlepaddle==3.1.1
paddleocr==3.2.0

# --- AI provider (Phase 7) ---
anthropic==1.1.0

# --- Authentication (Phase 10) ---
# bcrypt: minimal, actively-maintained password hashing library — used
# directly rather than through passlib's abstraction layer, since the
# project commits to exactly one hashing algorithm.
bcrypt==4.2.0
# PyJWT: minimal, actively-maintained JWT encode/decode.
PyJWT==2.9.0
# Required by Pydantic's EmailStr (used in auth request schemas).
email-validator==2.2.0

# --- Export (Phase 11) ---
openpyxl==3.1.5

# --- Testing ---
pytest==8.3.3
httpx==0.27.2

# --- Lint/format ---
ruff==0.6.9
